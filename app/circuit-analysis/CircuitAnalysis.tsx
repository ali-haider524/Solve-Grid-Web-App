"use client";

import { useState } from "react";
import ToolHeader from "../../components/ToolHeader";
import { solveOhmsLaw, solveRcTransient, solveResistorParallel, solveResistorSeries, solveTwoNodeNodal, solveVoltageDivider, type CircuitResult } from "../../lib/circuit-analysis";
import styles from "../../components/ResearchLab.module.css";

type Mode = "ohm" | "series" | "parallel" | "divider" | "rc" | "nodal";
const modes: Array<{ id: Mode; label: string }> = [
  { id: "ohm", label: "Ohm's law" }, { id: "series", label: "Series" }, { id: "parallel", label: "Parallel" }, { id: "divider", label: "Voltage divider" }, { id: "rc", label: "RC transient" }, { id: "nodal", label: "2-node nodal" },
];

function number(value: string) { const parsed = Number(value); return Number.isFinite(parsed) ? parsed : NaN; }
function parseResistors(value: string) { return value.split(/[\s,;]+/).filter(Boolean).map(Number); }

export default function CircuitAnalysis() {
  const [mode, setMode] = useState<Mode>("ohm");
  const [inputs, setInputs] = useState<Record<string, string>>({ voltage: "12", current: "", resistance: "100", resistors: "100, 220, 330", supply: "12", rTop: "1000", rBottom: "1000", capacitance: "100", time: "0.1", rcMode: "charge", r1: "1000", r2: "2000", r12: "1500", i1: "0.01", i2: "0.005" });
  const [result, setResult] = useState<CircuitResult | null>(null); const [error, setError] = useState("");
  const set = (key: string, value: string) => setInputs((current) => ({ ...current, [key]: value }));
  function run() { try { setError(""); let next: CircuitResult; if (mode === "ohm") next = solveOhmsLaw(inputs.voltage.trim() === "" ? NaN : number(inputs.voltage), inputs.current.trim() === "" ? NaN : number(inputs.current), inputs.resistance.trim() === "" ? NaN : number(inputs.resistance)); else if (mode === "series") next = solveResistorSeries(parseResistors(inputs.resistors), inputs.supply.trim() === "" ? NaN : number(inputs.supply)); else if (mode === "parallel") next = solveResistorParallel(parseResistors(inputs.resistors), inputs.supply.trim() === "" ? NaN : number(inputs.supply)); else if (mode === "divider") next = solveVoltageDivider(number(inputs.supply), number(inputs.rTop), number(inputs.rBottom)); else if (mode === "rc") next = solveRcTransient(inputs.rcMode === "discharge" ? "discharge" : "charge", number(inputs.supply), number(inputs.resistance), number(inputs.capacitance), number(inputs.time)); else next = solveTwoNodeNodal({ r1: number(inputs.r1), r2: number(inputs.r2), r12: number(inputs.r12), i1: number(inputs.i1), i2: number(inputs.i2) }); setResult(next); } catch (caught) { setResult(null); setError(caught instanceof Error ? caught.message : "Check the circuit values."); } }
  return <main id="main-content" className={styles.page}><ToolHeader active="research" /><section className={styles.hero}><p>DC CIRCUIT ANALYSIS LAB</p><h1>Work through circuit relationships with assumptions kept visible.</h1><span>Use ideal DC resistor models for Ohm&apos;s law, series and parallel networks, voltage dividers, RC transients, and a two-node nodal-analysis model.</span></section><section className={styles.workspace}><article className={styles.panel}><div className={styles.panelHeading}><div><p className={styles.eyebrow}>CIRCUIT WORKSPACE</p><h2>{modes.find((item) => item.id === mode)?.label}</h2></div><span className={styles.status}>IDEAL MODEL</span></div><div className={styles.tabRow}>{modes.map((item) => <button className={mode === item.id ? styles.activeTab : undefined} key={item.id} onClick={() => setMode(item.id)} type="button">{item.label}</button>)}</div>{mode === "ohm" ? <div className={styles.fieldGrid}><label><span>Voltage (V)</span><input inputMode="decimal" value={inputs.voltage} onChange={(event) => set("voltage", event.target.value)} placeholder="Leave one blank" /></label><label><span>Current (A)</span><input inputMode="decimal" value={inputs.current} onChange={(event) => set("current", event.target.value)} placeholder="Leave one blank" /></label><label><span>Resistance (Ω)</span><input inputMode="decimal" value={inputs.resistance} onChange={(event) => set("resistance", event.target.value)} placeholder="Leave one blank" /></label></div> : null}{mode === "series" || mode === "parallel" ? <div className={`${styles.fieldGrid} ${styles.oneColumn}`}><label><span>Resistors in ohms (comma-separated)</span><input value={inputs.resistors} onChange={(event) => set("resistors", event.target.value)} /></label><label><span>Optional supply voltage (V)</span><input inputMode="decimal" value={inputs.supply} onChange={(event) => set("supply", event.target.value)} /></label></div> : null}{mode === "divider" ? <div className={styles.fieldGrid}><label><span>Source voltage (V)</span><input inputMode="decimal" value={inputs.supply} onChange={(event) => set("supply", event.target.value)} /></label><label><span>Top resistance R1 (Ω)</span><input inputMode="decimal" value={inputs.rTop} onChange={(event) => set("rTop", event.target.value)} /></label><label><span>Bottom resistance R2 (Ω)</span><input inputMode="decimal" value={inputs.rBottom} onChange={(event) => set("rBottom", event.target.value)} /></label></div> : null}{mode === "rc" ? <div className={styles.fieldGrid}><label><span>Response</span><select value={inputs.rcMode} onChange={(event) => set("rcMode", event.target.value)}><option value="charge">Charging</option><option value="discharge">Discharging</option></select></label><label><span>Source voltage (V)</span><input inputMode="decimal" value={inputs.supply} onChange={(event) => set("supply", event.target.value)} /></label><label><span>Resistance (Ω)</span><input inputMode="decimal" value={inputs.resistance} onChange={(event) => set("resistance", event.target.value)} /></label><label><span>Capacitance (μF)</span><input inputMode="decimal" value={inputs.capacitance} onChange={(event) => set("capacitance", event.target.value)} /></label><label><span>Time (s)</span><input inputMode="decimal" value={inputs.time} onChange={(event) => set("time", event.target.value)} /></label></div> : null}{mode === "nodal" ? <><div className={styles.fieldGrid}><label><span>R1: node 1 to ground (Ω)</span><input inputMode="decimal" value={inputs.r1} onChange={(event) => set("r1", event.target.value)} /></label><label><span>R2: node 2 to ground (Ω)</span><input inputMode="decimal" value={inputs.r2} onChange={(event) => set("r2", event.target.value)} /></label><label><span>R12: between nodes (Ω)</span><input inputMode="decimal" value={inputs.r12} onChange={(event) => set("r12", event.target.value)} /></label><label><span>I1 injection (A)</span><input inputMode="decimal" value={inputs.i1} onChange={(event) => set("i1", event.target.value)} /></label><label><span>I2 injection (A)</span><input inputMode="decimal" value={inputs.i2} onChange={(event) => set("i2", event.target.value)} /></label></div><p className={styles.sectionNote}>Positive current is injected into the node. This is a 2-node resistive nodal-analysis model, not a full SPICE simulation.</p></> : null}<button className={styles.runButton} onClick={run} type="button">Calculate circuit <span>→</span></button>{error ? <p className={styles.error}>{error}</p> : null}</article><aside className={styles.resultCard} aria-live="polite"><p>ANALYSIS RESULT</p><h2>{result?.title ?? "Ready for circuit values"}</h2>{result ? <><div className={styles.resultRows}>{result.values.map((row) => <div key={row.label}><span>{row.label}</span><strong>{row.value}</strong></div>)}</div><div className={styles.methodNote}><p>MODEL NOTE</p><span>{result.note}</span></div></> : <div className={styles.empty}><span>V = IR</span><p>Select an ideal DC analysis mode and calculate a result.</p></div>}</aside></section></main>;
}
