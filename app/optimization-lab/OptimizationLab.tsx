"use client";

import { useMemo, useState } from "react";
import ToolHeader from "../../components/ToolHeader";
import { solveTwoVariableLinearProgram, type LinearConstraint, type LpResult } from "../../lib/linear-programming";
import styles from "../../components/ResearchLab.module.css";

type ConstraintInput = { a: string; b: string; operator: LinearConstraint["operator"]; c: string };
const initialConstraints: ConstraintInput[] = [
  { a: "2", b: "1", operator: "<=", c: "18" },
  { a: "2", b: "3", operator: "<=", c: "42" },
  { a: "3", b: "1", operator: "<=", c: "24" },
];

function coordinate(point: number) { return Number(point.toPrecision(7)); }

function LpChart({ result }: { result: LpResult }) {
  const points = result.feasiblePoints;
  if (!points.length) return null;
  const maxX = Math.max(1, ...points.map((point) => point.x)) * 1.2;
  const maxY = Math.max(1, ...points.map((point) => point.y)) * 1.2;
  const sx = (value: number) => 44 + value / maxX * 330;
  const sy = (value: number) => 220 - value / maxY * 175;
  const polygon = points.map((point) => `${sx(point.x)},${sy(point.y)}`).join(" ");
  return <figure className={styles.chartFrame}><p className={styles.chartTitle}>FEASIBLE CORNER POINTS</p><svg viewBox="0 0 410 250" role="img" aria-label="Feasible linear-programming corner points"><line x1="44" y1="220" x2="380" y2="220" stroke="#aab9d2"/><line x1="44" y1="220" x2="44" y2="28" stroke="#aab9d2"/>{points.length >= 3 ? <polygon points={polygon} fill="rgba(49,87,232,.16)" stroke="#3157e8" strokeWidth="2"/> : null}{points.map((point) => <g key={`${point.x}-${point.y}`}><circle cx={sx(point.x)} cy={sy(point.y)} r="4" fill={result.optimum?.x === point.x && result.optimum?.y === point.y ? "#e56a39" : "#3157e8"}/><text x={sx(point.x) + 6} y={sy(point.y) - 7} fill="#526681" fontSize="10">({coordinate(point.x)}, {coordinate(point.y)})</text></g>)}<text x="374" y="238" fill="#627590" fontSize="11">x</text><text x="28" y="34" fill="#627590" fontSize="11">y</text></svg><figcaption>Orange point indicates the selected optimum. The chart is a corner-point view, not a complete feasibility certificate.</figcaption></figure>;
}

export default function OptimizationLab() {
  const [objectiveX, setObjectiveX] = useState("3"); const [objectiveY, setObjectiveY] = useState("5"); const [direction, setDirection] = useState<"max" | "min">("max"); const [nonNegative, setNonNegative] = useState(true); const [constraints, setConstraints] = useState<ConstraintInput[]>(initialConstraints); const [result, setResult] = useState<LpResult | null>(null); const [error, setError] = useState("");
  const parsedConstraints = useMemo(() => constraints.map((item) => ({ a: Number(item.a), b: Number(item.b), c: Number(item.c), operator: item.operator })), [constraints]);
  function updateConstraint(index: number, key: keyof ConstraintInput, value: string) { setConstraints((items) => items.map((item, current) => current === index ? { ...item, [key]: value } : item)); }
  function run() { try { setError(""); setResult(solveTwoVariableLinearProgram({ objectiveX: Number(objectiveX), objectiveY: Number(objectiveY), direction, constraints: parsedConstraints, nonNegative })); } catch (caught) { setResult(null); setError(caught instanceof Error ? caught.message : "Check the optimisation model."); } }
  return <main id="main-content" className={styles.page}><ToolHeader active="research" /><section className={styles.hero}><p>OPTIMIZATION LAB</p><h1>Build a two-variable linear program, then inspect its feasible corner points.</h1><span>Set an objective, add constraints, choose maximize or minimize, and solve a transparent graphical linear-programming model in the browser.</span></section><section className={styles.workspace}><article className={styles.panel}><div className={styles.panelHeading}><div><p className={styles.eyebrow}>LINEAR PROGRAM</p><h2>Objective and constraints</h2></div><span className={styles.status}>2 VARIABLES</span></div><div className={`${styles.fieldGrid} ${styles.twoMobile}`}><label><span>Direction</span><select value={direction} onChange={(event) => setDirection(event.target.value as "max" | "min")}><option value="max">Maximize</option><option value="min">Minimize</option></select></label><label><span>Objective coefficient for x</span><input inputMode="decimal" value={objectiveX} onChange={(event) => setObjectiveX(event.target.value)} /></label><label><span>Objective coefficient for y</span><input inputMode="decimal" value={objectiveY} onChange={(event) => setObjectiveY(event.target.value)} /></label></div><div className={styles.switchRow}><span>Include non-negative constraints: x ≥ 0 and y ≥ 0</span><input checked={nonNegative} onChange={(event) => setNonNegative(event.target.checked)} type="checkbox" /></div><div className={styles.constraintList}>{constraints.map((constraint, index) => <div className={styles.constraintRow} key={index}><input aria-label={`Constraint ${index + 1} x coefficient`} inputMode="decimal" value={constraint.a} onChange={(event) => updateConstraint(index, "a", event.target.value)} /><input aria-label={`Constraint ${index + 1} y coefficient`} inputMode="decimal" value={constraint.b} onChange={(event) => updateConstraint(index, "b", event.target.value)} /><select aria-label={`Constraint ${index + 1} operator`} value={constraint.operator} onChange={(event) => updateConstraint(index, "operator", event.target.value)}><option value="<=">≤</option><option value=">=">≥</option><option value="=">=</option></select><input aria-label={`Constraint ${index + 1} constant`} inputMode="decimal" value={constraint.c} onChange={(event) => updateConstraint(index, "c", event.target.value)} />{constraints.length > 1 ? <button className={styles.removeButton} aria-label="Remove constraint" onClick={() => setConstraints((items) => items.filter((_, current) => current !== index))} type="button">×</button> : <span />}</div>)}</div>{constraints.length < 8 ? <button className={styles.smallButton} onClick={() => setConstraints((items) => [...items, { a: "", b: "", operator: "<=", c: "" }])} type="button">+ Add constraint</button> : null}<button className={styles.runButton} onClick={run} type="button">Solve linear program <span>→</span></button>{error ? <p className={styles.error}>{error}</p> : null}</article><aside className={styles.resultCard} aria-live="polite"><p>OPTIMUM</p><h2>{result?.status === "optimal" ? `${direction === "max" ? "Maximum" : "Minimum"} objective` : "Ready to solve"}</h2>{result?.optimum ? <><strong className={styles.resultValue}>Z = {coordinate(result.optimum.value)} at ({coordinate(result.optimum.x)}, {coordinate(result.optimum.y)})</strong><div className={styles.resultRows}>{result.feasiblePoints.map((point) => <div key={`${point.x}-${point.y}`}><span>Corner ({coordinate(point.x)}, {coordinate(point.y)})</span><strong>Z = {coordinate(point.value)}</strong></div>)}</div><LpChart result={result} /><div className={styles.methodNote}><p>METHOD</p><span>{result.note}</span></div></> : <div className={styles.empty}><span>max Z</span><p>Enter coefficients and constraints, then solve the two-variable model.</p></div>}</aside></section></main>;
}
